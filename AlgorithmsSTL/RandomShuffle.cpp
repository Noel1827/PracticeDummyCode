#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <numeric>
#include <array>
using namespace std;

void couts(int c){
cout << c << " " ;
}

void stringcouts( string c){
    cout << c << " ";
}


int main()
{
    // vector<int> v(10);
    // iota(v.begin(), v.end(), 0);

    // for_each(v.begin(), v.end(), couts);

    // cout << "\n";

    // random_shuffle(v.begin(), v.end());

    // for_each(v.begin(), v.end(), couts);


    // vector<string> bitch = {"Noel" , "Myrian", "Alonda" , "Mery" , "Marcela"};
    //   for_each(bitch.begin(), bitch.end(), stringcouts );
    //   cout << "\n";

    // random_shuffle(bitch.begin(), bitch.end());

    // for_each(bitch.begin(), bitch.end(), stringcouts );
    

    return 0;
}